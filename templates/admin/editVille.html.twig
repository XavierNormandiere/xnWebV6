{% extends 'base.html.twig' %}

{% block body %}
<h1>Modofocation informations villes</h1>
    {{ form_start(formVille) }}
    {{ form_widget(formVille) }}
    <button type="submit" class="btn btn-warning">Modifier</button>
    <button type="button" class="btn btn-dark"><a href="{{ path('ville_villes') }}">Retour</a></button>

    {{ form_end(formVille) }}
{% endblock %}

{% block title %}
Modofoer ville
{% endblock %}

