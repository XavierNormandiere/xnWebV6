<nav class="navbar navbar-expand-lg navbar-light bg-gradient bg-dark">
    <div class="container-fluid">

        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active text-white" aria-current="page" href="{{ path('main_home') }}">Home Page | </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('contact_formulaire') }}"> Contact |</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('login') }}"> login |</a>
                </li>

                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link text-white" href="{{ path('visite_visites') }}"> Voyages | </a>
                    </li>
                <li class="nav-item">
                    <a class="nav-link text-white" href="{{ path('app_logout') }}"> logout {{ app.user.prenom }}|</a>
                </li>
                {% if is_granted("ROLE_ADMIN") %}
                    <li class="nav-item">
                        <a class="nav-link text-white" href="{{ path('app_register') }}"> Register |</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="{{ path('admin_admin') }}">Interface Admin |</a>
                    </li>
                {% endif %}
                {% endif %}

            </ul>
        </div>
    </div>
</nav>