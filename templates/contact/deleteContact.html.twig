{% extends 'base.html.twig' %}

{% block body %}
<h1>Supprimer contact</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-warning">Supprimer</button>
    <button type="button" class="btn btn-danger"><a href="{{ path('contact_contacts') }}">Retour liste contacts</a> </button>

    {{ form_end(form) }}
{% endblock %}

{% block title %}
Delete contact {{ parent() }}
{% endblock %}

